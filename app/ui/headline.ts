import { css } from 'remix/ui'

// The page heading on every left-aligned header: company, tech, location and 404.
export const headlineStyle = css({
	margin: 0,
	fontSize: '1.375rem',
	fontWeight: 600,
	lineHeight: 1.1,
	letterSpacing: '-0.02em',
})
